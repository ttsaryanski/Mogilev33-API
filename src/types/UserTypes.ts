export interface UserResponseType {
    _id: string;
    email: string;
    role: string;
    createdAt: Date;
}
export interface LoginUserResponseType {
    _id: string;
    email: string;
    password: string;
    role: string;
    createdAt: Date;
}
